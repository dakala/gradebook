# Schema for the configuration files of the Gradebook module.

grade_letter.set.*:
  type: config_entity
  label: 'Grade letter settings'
  mapping:
    id:
      type: string
      label: 'ID'
    label:
      type: label
      label: 'Label'
